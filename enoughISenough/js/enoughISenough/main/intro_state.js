class IntroState extends PP.State {
    update(dt, fsm) {
        this._myFSM = new PP.FSM();
    }

    init(fsm) {
    }

    start(fsm, transitionID) {
    }

    end(fsm, transitionID) {
    }
}